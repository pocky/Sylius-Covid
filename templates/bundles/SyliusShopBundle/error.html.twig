{% extends '@SyliusShop/layout.html.twig' %}

{% block content %}
    <h2 class="ui center aligned icon header">
        <img src="{{ asset('build/shop/logo.png', 'shop') }}" alt="{{ sylius.channel.name }}" class="ui small image" />
        <br/><br/>
        {{ 'sylius.ui.unexpected_error_occurred'|trans }}
    </h2>

{% endblock %}
